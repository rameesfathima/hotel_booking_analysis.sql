# hotel_booking_analysis.sql
Hotel Booking Data Analysis using MySQL. This project demonstrates SQL data analysis techniques including SELECT, WHERE, ORDER BY, GROUP BY, aggregate functions, JOINs, subqueries, views, and indexes. The project includes SQL queries and output screenshots to showcase practical database analysis and insights from hotel booking data.
